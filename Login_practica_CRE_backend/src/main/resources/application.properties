spring.application.name                                 = Login_practica_CRE_backend


# ===============================
# BASE DE DATOS LOCAL (MySQL)
# ===============================
# spring.datasource.url=jdbc:mysql://localhost:3306/capacitacion_CRE
# spring.datasource.username=root
# spring.datasource.password=123456
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ===============================
# Configuracion de Aiven MySQL
# ===============================
# Cambiamos localhost por el hostname de Aiven y el puerto
spring.datasource.url=jdbc:mysql://login-idati-idaticre.i.aivencloud.com:28299/defaultdb?sslMode=REQUIRED

spring.datasource.username                              = avnadmin
spring.datasource.password                              = AVNS_QGoi7chfSWeMC1dCndi
spring.datasource.driver-class-name                     = com.mysql.cj.jdbc.Driver

# SSL requerido por Aiven
spring.datasource.hikari.data-source-properties.sslMode = REQUIRED

# ===============================
# JPA / Hibernate
# ===============================
# IMPORTANTE: Cambia 'validate' a 'update' la primera vez para que
# Hibernate cree las tablas en Aiven automaticamente.
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform                            = org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql                                     = true

spring.jpa.hibernate.ddl-auto                           = validate

# ===============================
# Correo Gmail SMTP
# Puerto 587 es el estandar para SMTP con STARTTLS
# Puerto 465 seria para SSL directo
# ===============================
spring.mail.host                                        = smtp.gmail.com
spring.mail.port                                        = 587

# Contrasena de aplicacion generada en Google
spring.mail.username                                    = idaticre@gmail.com
# NO es tu contrasena normal de Gmail
spring.mail.password                                    = vjub dunl zolq kcvd

# Requiere autenticacion para enviar correos
spring.mail.properties.mail.smtp.auth                   = true
# STARTTLS: inicia la conexion sin cifrado y la eleva a cifrada
spring.mail.properties.mail.smtp.starttls.enable        = true
# Es el mecanismo de seguridad que usa Gmail en el puerto 587
spring.mail.properties.mail.smtp.starttls.required      = true
spring.mail.properties.mail.smtp.connectiontimeout=3000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=3000
